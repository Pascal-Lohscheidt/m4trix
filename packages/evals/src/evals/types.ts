/** Matches a tag by exact string equality or regex test */
export type TagMatcher = string | RegExp;

/** Matches a file path by glob string or regex test */
export type PathMatcher = string | RegExp;
